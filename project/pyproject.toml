[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
# E9  = syntax / implied runtime errors
# F   = pyflakes: unused imports, undefined names, unused variables
select = ["E9", "F"]
